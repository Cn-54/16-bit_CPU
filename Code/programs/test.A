; ==========================================
; Wait for Enter, then halt
; ==========================================

.ORG 0x1000

START:
    ; Compare input with Enter (0x0A)
    MOVI R1, 10
    CMP R14, R1

    ; Enter pressed -> exit
    JE EXIT

    ; Otherwise keep waiting
    JMP START


EXIT:

    HALT